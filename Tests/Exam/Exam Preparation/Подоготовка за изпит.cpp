

#include <iostream>
#include"Header.h"
using namespace std;

//void own_sqrt(int num) {
//	int x;
//	num = pow(x, 2);
//	cout << x;
//}

int main() {
	/*point a1, a2, a3;
	cin >> a1.x >> a2.y;
	cin >> a2.x >> a2.y;
	cin >> a3.x >> a3.y;
	cout << calclen(a1, a2);*/


	//int num = 16;
	//own_sqrt(num);
	//cout << num;



	int arr[5];

	for (int i = 0; i < 5; i++) {
		cin >> arr[i];
	}

	for (int i = 0; i < 5; i++) {
		if(arr[i]> INT32_MIN&)
	}
}


